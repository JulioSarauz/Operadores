package com.coac.operador.client;

public class SearchClient {
  /*  private final RestTemplate restTemplate = new RestTemplate();
    private final String SEARCH_SERVICE_URL = "http://ms-books-search/validate";

    public boolean validateItems(List<Item> items) {
        ResponseEntity<Boolean> response = restTemplate.postForEntity(SEARCH_SERVICE_URL, items, Boolean.class);
        return response.getBody();
    }*/
}
